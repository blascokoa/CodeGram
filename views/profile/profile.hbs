<div id="profil-view">
    <h2>{{myProfile.name}}'s PROFILE</h2>

    <img src="{{#if myProfile.profile_pic}}{{myProfile.profile_pic}}{{else}}"./images/image.jpg"{{/if}}" alt="profile's picture">
    <form action="/profile/edit">
        <button>Edit</button>
    </form>
    <h4>{{myProfile.username}}</h4>
    <h4>Bio??</h4>

</div>